{
  "exec-opts": ["native.cgroupdriver=systemd"],
  "log-driver": "json-file",
  "log-opts": {
    "max-size": "100m"
  },
  "storage-driver": "overlay2"
{%- if docker_network and docker_subnet_mask %},
  "default-address-pools":
    [
      { 
        "base":"{{ docker_network }}","size":{{ docker_subnet_mask }} 
      }
    ] 
{% endif %}
}
